{
  "mcpServers": {
    "etoro-public-api": {
      "url": "https://mcp.public-api.etoro.com",
      "headers": {
        "x-api-key": "${env:ETORO_API_KEY}",
        "x-user-key": "${env:ETORO_USER_KEY}"
      }
    }
  }
}
